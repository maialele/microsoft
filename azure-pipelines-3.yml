trigger:
- main

pool:
  name: 'default'
  vmImage: 'agent1'

steps:
- task: AzurePowerShell@5
  displayName: 'Create storage accounts'
  inputs:
    azureSubscription: 'MSEC_Candidates_Homeworks_new'
    ScriptType: 'FilePath'
    FilePath: 'deployStorage_Server.ps1'
